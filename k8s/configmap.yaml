apiVersion: v1
kind: ConfigMap
metadata:
  name: rentorbit-config
  namespace: ${KUBE_NAMESPACE}
  labels:
    app.kubernetes.io/name: rentorbit
    app.kubernetes.io/part-of: rentorbit
data:
  NODE_ENV: "production"
  WEB_PORT: "3000"
  API_PORT: "4000"
  APP_URL: "${APP_URL}"
  NEXT_PUBLIC_API_URL: "${NEXT_PUBLIC_API_URL}"
  API_INTERNAL_URL: "${API_INTERNAL_URL}"
  OPENAI_INTELLIGENCE_MODEL: "gpt-4.1-mini"
  INTELLIGENCE_SESSION_TTL_MS: "60000"
  INTELLIGENCE_PROFILE_DIR: "/tmp/rentorbit/intelligence/listings"
